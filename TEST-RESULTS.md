# 測試與驗收紀錄

最後驗收日期：2026-09-11  
版本：TW-BETA-0.2（四選一）

## 自動化檢查

- `pnpm test`：16／16 通過。
- `pnpm lint`：通過，0 error。
- `pnpm exec tsc --noEmit`：通過。
- `pnpm build`：通過；正式路由僅 `/`、`/api/session`、`/api/score`。
- 題庫：2 題練習、40 題正式；A／B／C／D 正解位置各 10 題。
- 答案序列：無超過限制的連續位置，也沒有簡單交替規律。
- 公開 session payload 不含正解、內部解析、干擾項理由或 IRT 參數。
- 3PL 猜測參數為 0.25；EAP、後驗標準誤、參考區間及上下限顯示皆有固定測試。

## 三次完整端到端作答

| 流程 | 寬度 | 結果 | 驗收重點 |
|---|---:|---|---|
| 全部正確 | 320 px | `145 以上` | 40 題可完成；上限不輸出假精度 |
| 全部錯誤 | 390 px | `低於 70` | 40 題可完成；下限不輸出假精度 |
| 前 15 題正確、其餘錯誤 | 360 px | 98；區間 86～111 | 第 1 題以鍵盤作答；第 10 題後重新整理並由第 11 題續答 |

自動化點擊會產生不合理的極短反應時間，因此上述三次結果正確顯示「作答狀態不穩定」並放寬區間；這是預期行為，不是計分錯誤。

## 手機與視覺檢查

- 320 px：逐頁檢查全部 40 題；無水平溢出，選項在 844 px 高度下皆至少 88 px。
- 360 px：完成混合作答、鍵盤輸入、重新整理續答與結果頁。
- 390 px：完成全錯流程並檢查四選一排版。
- 430 px：由響應式規則與最大內容寬度檢查；主要內容維持置中且不超過 520 px。
- 逐題檢查 semantic segments；沒有單一標點、關鍵短語或數字單位被程式強制拆開。
- 修正第 23 題摺紙 SVG 的標籤與孔洞對比後重新截圖檢查。
- 色彩對比：四個選項搭配白字分別約 6.23、5.24、6.33、6.96:1；主文字對背景約 13.95:1，均達 WCAG AA。
- 灰階可判讀：題目規則同時以輪廓、填充、方向、數量或位置傳達，不只依賴顏色。

## 效能觀察

- 390 × 844 px、開發預覽首屏觀察：LCP 約 396 ms、CLS 0.0000、TTFB 約 212 ms。
- 不載入外部字型或大型照片；插畫為本地小型資產，題目圖形為 inline SVG／程式化圖形。
- 正式建置最大應用專屬 client chunk 約 80 KB（未壓縮）；共享框架另行分包。
- 未宣稱正式 Lighthouse 分數；最終仍建議在實際部署節點以 Lighthouse Mobile 複測網路與主機因素。

## 已知測量限制

此為未經台灣成人代表性樣本校準的 Beta 模型。暫定 IRT 參數、各向度描述與模型參考區間均不可視為臨床常模、正式 IQ 或同齡 IQ；詳見 `ASSESSMENT.md`。
